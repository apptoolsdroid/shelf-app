// ============================================================================
// Single source of truth for the app version.
//
// Bump this (and the matching CACHE_NAME in sw.js) whenever you deploy a
// change. The version is shown in the top bar, so you can always tell at a
// glance which build your tablet is actually running — which matters for an
// installed web app, where an old copy can otherwise sit in the cache and
// look identical to a new one.
// ============================================================================
export const APP_VERSION = "1.5.0";
